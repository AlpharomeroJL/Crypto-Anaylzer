# venvs
.venv/
venv/

# env files (may contain CRYPTO_DB_PATH, secrets; never commit)
.env
.env.local
.env.*.local

# runtime artifacts (do not commit)
*.sqlite
*.sqlite3
plots/
logs/
archive/
reports/
__pycache__/
*.pyc

# pytest cache (local test run artifacts)
.pytest_cache/

# deterministic rerun test temp dirs (do not commit)
tmp_rerun_1/
tmp_rerun_2/

# profile test output dirs (do not commit)
tmp_profile_off/
tmp_profile_on/

# migration backup artifacts (do not commit)
*.sqlite.bak*

# streamlit
.streamlit/

# OS
.DS_Store
Thumbs.db

# local backup copies (avoid accidental commit of temp edits)
*.md.bak

# private docs (never push to GitHub)
docs/private_conversion.md

# Service logs live outside repo (not trackable here):
# C:\ProgramData\CryptoAnalyzer\poller.log
# C:\ProgramData\CryptoAnalyzer\poller_error.log
